global:
  resolve_timeout: 5m

route:
  receiver: caudals-local
  group_by:
    - alertname
    - job
    - severity
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: caudals-local
