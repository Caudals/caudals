import { redirect } from "next/navigation";import { requirePageIdentity } from "@/components/evals/page-identity";import { EvalShell } from "@/components/evals/shell";import { QueueView } from "@/components/evals/operator-overview";import { AssessmentReviewQueue } from "@/components/evals/assessment-review";
export default async function Page(){const identity=await requirePageIdentity("/ops/review");if(!identity.platformRole)redirect("/workspace/evaluations");return <EvalShell identity={identity}><QueueView kind="review"/><AssessmentReviewQueue/></EvalShell>;}
