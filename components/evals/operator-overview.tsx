"use client";
import Link from "next/link";import { useEffect,useState } from "react";import { evalRequest } from "./api";import { Badge,DataTable,EmptyState,InlineSelect,Loading,PageHeading,RowTitle,Status,StatusBadge } from "./primitives";
import { Activity,FileText,SlidersHorizontal } from "lucide-react";import { t } from "@/lib/evals/messages/en";
type Workspace={id:string;name:string};type Managed={evaluations:Array<{id:string;title:string;preparation_status:string;project_title:string}>;runs:Array<{id:string;evaluation_title:string;status:string;phase:string;updated_at:string}>;reports:Array<{id:string;title:string;publication_status:string}>};
export function OperatorOverview(){return <QueueView kind="overview"/>;}
export function QueueView({kind}:{kind:"overview"|"review"|"reports"|"platform"}){const [workspaces,setWorkspaces]=useState<Workspace[]>([]),[selected,setSelected]=useState(""),[data,setData]=useState<Managed|null>(null),[error,setError]=useState("");useEffect(()=>{void evalRequest<Workspace[]>("/workspaces").then(items=>{setWorkspaces(items);if(items[0])setSelected(items[0].id);}).catch(()=>setError(t("error")));},[]);useEffect(()=>{if(!selected)return;void evalRequest<Managed>(`/evaluations?orgId=${encodeURIComponent(selected)}`).then(setData).catch(()=>setError(t("error")));},[selected]);const title=kind==="review"?t("reviewQueue"):kind==="reports"?t("reports"):kind==="platform"?t("platform"):t("operatorOverview");return <><PageHeading title={title}>{kind==="platform"?t("platformHelp"):t("operatorOverviewHelp")}</PageHeading>{error&&<Status error>{error}</Status>}<div className="eval-toolbar"><InlineSelect id="ops-client" label={t("selectedClient")} value={selected} onChange={event=>setSelected(event.target.value)}><option value="">{t("chooseWorkspace")}</option>{workspaces.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</InlineSelect></div>{kind==="platform"?<div className="eval-panel"><h2>{t("providersBudgets")}</h2><p>{t("providerSecurityHelp")}</p></div>:!data?<Loading />:kind==="reports"?(data.reports.length?<DataTable caption={t("reports")} headers={[t("report"),t("statusLabel")]}>{data.reports.map(item=><tr key={item.id}><RowTitle>{item.title}</RowTitle><td><StatusBadge value={item.publication_status} /></td></tr>)}</DataTable>:<EmptyState title={t("noReports")} icon={<FileText />}><p>{t("operatorOverviewHelp")}</p></EmptyState>):kind==="review"?(()=>{const queue=data.evaluations.filter(item=>["needs_review","validating"].includes(item.preparation_status));return queue.length?<DataTable caption={t("reviewQueue")} headers={[t("evaluation"),t("statusLabel")]}>{queue.map(item=><tr key={item.id}><RowTitle meta={item.project_title}>{item.title}</RowTitle><td><StatusBadge value={item.preparation_status} /></td></tr>)}</DataTable>:<EmptyState title={t("reviewQueue")} icon={<SlidersHorizontal />}><p>{t("operatorOverviewHelp")}</p></EmptyState>;})():(data.runs.length?<DataTable caption={t("activeRuns")} headers={[t("evaluation"),t("statusLabel"),t("phase"),{label:t("access"),align:"end"}]}>{data.runs.map(item=><tr key={item.id}><RowTitle>{item.evaluation_title}</RowTitle><td><StatusBadge value={item.status} /></td><td><Badge>{item.phase.replaceAll("_"," ")}</Badge></td><td className="p-table-action"><Link className="p-link" href={`/ops/runs/${item.id}?orgId=${selected}`}>{t("inspect")}</Link></td></tr>)}</DataTable>:<EmptyState title={t("noRuns")} icon={<Activity />}><p>{t("noRunsHelp")}</p></EmptyState>)}</>;}
