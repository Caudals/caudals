# WP-00 — Contracts and platform inventory

Status: ready_for_review. Specification: draft v0.1, base commit `9c5b33b`.
User explicitly assigned Stage A on 2026-09-17. No production service or data
was changed during inventory. Development containers are separately named.

## Observed environment (2026-09-17)

- Host: `caudals-1`; current development already runs on the VPS. Approved
  remote access: `caudals@caudals-1` over Tailscale. No public SSH required.
- Four CPUs; 7,751 MiB RAM, 3,913 MiB initially available; swap 3,782/4,095 MiB
  used. Root disk 75 GiB, 8.7 GiB free (88% occupied). These are observations,
  not guaranteed capacity. Run one bounded build/test process at a time.
- Active Swarm services: caudals app, Leads, growth-social, HyperFrames renderer,
  PostgreSQL. Dokploy services scaled down. Preserve all existing services.
- PostgreSQL 16.13. Extensions: citext, pg_cron, pg_stat_statements, pg_trgm,
  pgcrypto, plpgsql, vector. No new evaluation dependency on optional extensions.
- Production app image tag matches base `9c5b33b`; immutable workflow is
  `.github/workflows/deploy.yml` and `scripts/deploy-app-stack.sh`, using Docker
  secret runtime env. App image currently uses Node 20; development Node 22.23.2.
- Private MinIO service is not active. Existing image is available for a
  disposable loopback test service. Production storage provisioning is a release
  prerequisite, not silently performed here.
- Local backups exist. Off-host encrypted copy freshness and restore health are
  **unverified**; release requires independent backup/restore evidence. Filenames
  alone are not backup validity evidence.
- DGX route is interface `dgxspark`, observed type WireGuard. Approved network
  transport is this existing encrypted private tunnel, not a private-IP trust
  exception for customer destinations. GET `/v1/models` succeeded without
  inference. Observed IDs include `gemma4:26b`, `qwen3-coder:30b`, `gpt-oss:20b`,
  `qwen3-embedding:0.6b`, `qwen2.5-coder:7b`, `llama3.1:70b` and aliases.
  Context/tool/JSON/usage capabilities and throughput remain unknown until
  individual budgeted probes. No weights downloaded.

## Contracts and plan

- Host routing: `proxy.ts`; old surface gates retained. Identity is Better Auth
  in `lib/auth`, old operator lookup maps by email. Evaluation identity instead
  uses explicit `evals.platform_role` and `membership` linked to auth user IDs.
- Migration history ends at 030, with pre-existing duplicate 024 prefixes.
  Do not reorder it. Foundation adds 031 identity, 032 evidence, 033 execution.
  Apply with a migration advisory lock; production remains untouched.
- Runtime uses EVALS_DATABASE_URL(_FILE), a separate non-owner NOBYPASSRLS
  login. Identity provisioning functions run as trusted migration owner, have
  fixed search paths, explicit checks and revoked PUBLIC execution.
- Worktree `/home/caudals/evals-foundation`, branch `codex/evals-foundation`.
  No production env copied. Synthetic auth users, dedicated PostgreSQL container
  on loopback 55439; private test MinIO on loopback 55440. Preview loopback only.
- Resource plan: test Postgres 256 MiB/0.5 CPU; test MinIO 192 MiB/0.25 CPU;
  single preview/build at <=2 GiB heap. Stage A general worker starts with one
  inference slot. Browser/document worker production deployment deferred until
  capacity is measured again; current disk/swap pressure precludes asserting
  all proposed workers fit with 30% headroom.
- New routes and English app catalog are authorized in root guidance,
  architecture/frontend/design contracts and `docs/evals/AGENTS.md`.

## Verification commands actually run

`hostname`; `free -m`; `df -h /`; `nproc`; `sudo -n docker service ls`;
`sudo -n docker stats --no-stream` (bounded name/CPU/memory formatting);
private-container `psql` SELECT version()/extension names; `ip route get` and
`ip -d link show dgxspark`; bounded GET model inventory; file-name-only local
backup inventory. No credentials printed. Integration/test commands for later
packages are recorded in their handoffs.

## References and adoption

See `../references.md`. pg-boss 10.3.3 is pinned for compatibility with the
existing Node 20 image and PostgreSQL 16, using its MIT license. No framework,
model weights or public benchmark datasets were adopted.

Acceptance: routing/auth/deploy/migration entry points documented; production
unchanged during the original inventory; concrete headroom constraints recorded;
observed/unknown DGX facts separated. Next: WP-01. MinIO and the synthetic
private-storage probe are now recorded in WP-08. Off-host recovery is deferred
on the user's direction.
