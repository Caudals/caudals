# WP-08 — Managed release, lifecycle and recovery

Status: implementation_ready; full production release gate remains open. The 2026-09-23 entitlement-lock incident repair is deployed separately; it does not satisfy the worker, backup/restore, authorized-target, artifact or PDF acceptance gates below.

## Delivered operator flow

`/ops` now exposes client selection, evaluation preparation, review, runs, reports and platform navigation. `/ops/evaluations` creates clients/projects, connects an API, creates evaluations and previews response imports. `/ops/review` projects preparation review states. `/ops/runs/:id` shows the frozen plan hash, phase/timeline, case-unit outcomes/reasons/attempt IDs and reserved/settled/unresolved costs. `/ops/reports` shows publication state. `/ops/platform` describes the separate privileged provider/budget control boundary rather than exposing secrets.

The complete API path is client → project/source/target → context questions → grounded suite draft → explicit freeze → connection/import → durable run → score/review → immutable report → publish → authenticated export/private share. `npm run evals:demo-stage-b` runs the clearly labeled synthetic calculation slice without customer data or an external call.

## Lifecycle and recovery controls

- `retention_job` separates artifact expiry/deletion, share revocation and report withdrawal. Actions append minimal immutable `recovery_control_event` records; artifact objects are deleted before metadata becomes `deleted`.
- Recovery control events cover share, credential, artifact, report and workspace actions. `npm run evals:export-recovery-ledger -- /absolute/private/path.jsonl` creates a new mode-0600 ledger file for independently retained encrypted storage.
- `npm run evals:recovery-rehearsal -- <snapshot-UTC>` refuses to run with dispatch enabled, imports the newer ledger, replays controls before reads, records verification evidence and leaves workers paused. `replayRecoveryControls` never restores deleted content or re-enables a revoked grant/credential. Both commands are registered in `package.json` and load the React-server condition required by the `server-only` lifecycle module.
- `npm run evals:release-check` verifies Stage B migrations and non-privileged service roles while dispatch is disabled. `--dispatch-reviewed` is required to inspect a configuration with dispatch enabled.
- Migrations 034–038 are additive. Their down files refuse destructive rollback. Roll back code by disabling queues and restoring the prior compatible image; preserve attempts, costs, evidence and lifecycle records.

## Release procedure

1. Build immutable web, general-worker and Playwright document-worker images on a runner with adequate memory. Do not build under host pressure.
2. Verify a recent encrypted off-host backup and execute the restore rehearsal with the independently retained control ledger.
3. Apply 034–038 with the explicit migration-owner command and advisory lock; apply `worker-grants.sql` and `document-grants.sql` only to their separately provisioned NOBYPASSRLS service roles.
4. Deploy workers with dispatch/documents disabled, explicit workspace allowlists, private DB/queue/storage credentials, resource limits and log rotation.
5. Run tenant/host/secret/budget tests, storage probe, a synthetic demo and one owner-authorized tiny API evaluation. Visually inspect its PDF.
6. Enable only the evaluated workspace, watch queue age, leases, target errors, spend reconciliation, disk/RAM and backup age, then perform a second compatible run/comparison.
7. Confirm marketing and Leads CRM health before widening invite-only access.

Production-only evidence still required: actual private storage artifact round-trip, authorized real target result, PDF visual inspection, immutable image digests, current backup/restore proof, resource headroom and no regression to marketing/CRM latency. These are stop/go checks, not implementation placeholders.

## Local verification record

## Production incident repair — 2026-09-23

The live workspace summary failed because the app runtime selected a workspace
entitlement `FOR SHARE` directly, which requires `UPDATE` privilege even though
the runtime intentionally has read-only access to that table. Additive migration
045 now exposes a narrowly scoped `SECURITY DEFINER` lock function that checks
the current tenant and actor before taking the row lock. The runtime role still
has no direct write privilege on workspace entitlements. The database function
was applied under the migration advisory lock and runtime access was verified;
the compatible app change uses it through `stage-c.ts`.

The four supplied UI failures also prompted safe request-ID error references,
generic server errors that do not expose exception detail, and retry/empty/error
states on operator queues, workspace reports and improvement datasets. Coverage
includes a PostgreSQL privilege fixture, API error redaction tests and the
evaluation UI contract. The change was pushed to `main` as `3a43221` and
deployed on `caudals-1` as image
`mariomedpar/caudals:3a43221b7cb1689d90a0da6313540bbbfc047572`. The deployment
script's in-container and public health checks passed; a follow-up live check
confirmed the service is running that image, `/` and `/ops/reports` return 200,
and the unauthenticated workspace API retains its expected 401 boundary. No
authenticated live customer session was available for a post-deploy UI pass.
These checks do not replace the production release gates above.

- PostgreSQL 16 applied 031–038 from scratch: 70 Stage A/B tables after the target-attempt addition (rerun during final verification).
- Full isolated suite with a non-owner runtime, the Stage B target worker and real pg-boss/SIGKILL fixtures: 163 passed, 2 storage tests skipped pending an explicit disposable S3 endpoint.
- The Playwright evaluation UI contract passed 24 browser scenarios, including authentication return paths, mobile shell behavior and retry/idempotency behavior; the proxy regression suite passed 3 tests.
- TypeScript and ESLint passed after Stage B wiring. The final handoff should use the latest counts recorded by the closing verification run.
- The recovery CLI replayed a newer synthetic artifact-deletion control into an isolated restored PostgreSQL database with dispatch paused; the restored artifact ended in `deleted` and the rehearsal recorded `passed`.
- On 2026-09-23, the registered recovery command restored the 2026-09-20 evaluation archive into a disposable PostgreSQL 16 container, then replayed one synthetic workspace-deletion control with dispatch paused. The rehearsal recorded `passed` and `ledger_replayed_through=1`; the live snapshot exported zero later control events and contained zero deleted artifacts/revoked shares. The archive is a same-host, pre-045 snapshot, and the isolated restore used minimal `auth_user` identity stubs for the evals foreign keys. This verifies the CLI and replay path, not a full identity/storage restore or the required encrypted off-host backup.
