# WP-07 — Reports, exports and private delivery

Status: deployed. Automatic preliminary reports, the bounded narrative writer, PDF/CSV/CEF exports, bearer sharing with revocation, comparison and persisted findings are live.

## Delivered

- Reports are immutable typed snapshots: system/scope, exact denominators, findings, result evidence, improvements, methodology, frozen IDs, review status and limitations. Narrative takeaways are derived only from snapshot findings; narrative failure does not block the deterministic report.
- Workspace report UI provides overview, findings, result transcript/evidence, improvements and methodology tabs from that one snapshot. Trusted HTML rendering escapes candidate text and uses print-safe headers, wrapping and page-break rules.
- CSV neutralizes spreadsheet formula prefixes and distinguishes empty values from numeric zero. CEF export is JSONL containing a checksum manifest plus the exact case, observation and assessment documents—not a renamed report JSON object.
- PDF requests create durable `export_job` rows. `services/evals-documents/main.ts` claims only explicit workspaces, recovers stale render-only work, renders in isolated Chromium, stores private checked artifacts and emits one deduplicated completion notification. Report/scoring work is not retried when rendering fails.
- Publication switches only the report's current immutable revision and emits a deduplicated in-app notification. Prior revisions do not mutate.
- Bearer shares use 256-bit URL-safe tokens stored only as hashes, bounded expiry, report-revision scope, allowlisted projection fields, access audit and immediate revocation. Only grants explicitly marked `bearer` resolve through the public exchange. The token is read from the URL fragment, exchanged by same-origin POST, then removed from browser history. Share routes send no-referrer/no-store/noindex/CSP headers and expose no mutation API or private object URL.

## API contract

- Create/read/publish: `/api/evals/v1/reports`, `/reports/:id`, `/reports/:id/publish`.
- Queue PDF or create CSV/CEF artifact: `POST /exports`; authenticated bytes stream through `/report-artifacts/:id` after a fresh authorization check.
- Create/revoke a share: `/reports/:id/shares`, `/shares/:id`; exchange bearer token: `/share/exchange`.

Workspace and named-recipient access use authenticated workspace/report authorization. A named-recipient token is deliberately not accepted by the bearer exchange.

## Verification

Tests assert identical snapshot counts in web/HTML/CSV, HTML/formula escaping, hash-only tokens, allowlisted projections and checksummed CEF records. The production document worker reported `document_worker_ready` on 2026-09-23. The private MinIO probe wrote, read and deleted a synthetic object successfully. A production synthetic evaluation queued a PDF export, which the document worker completed and the authenticated app gateway delivered. The PDF was visually inspected. A second queued export after the renderer change included the actual case input, system answer and assessment alongside the result and source IDs. Its two-page A4 output was downloaded through the live app and visually inspected without clipping.

The `--long` synthetic renderer fixture produced an 11-page A4 PDF with twelve interactions and long responses. Text extraction found all twelve responses, and the first, middle and last pages were visually checked for page breaks and clipping. The fixture ran the production renderer in the pinned document image with no network access and no customer data. A bearer share returned only selected report fields; revocation made the same token fail with HTTP 404. The share creation failure caused by passing a PostgreSQL array to a JSONB column was repaired by explicit JSON serialization and verified against the live app.

## Production closure — 2026-09-25 (UTC)

- `lib/evals/reports/narrative.ts` and the report-writer job produce executive takeaways only from an evidence packet derived from the snapshot. Each takeaway must cite snapshot findings or assessments, and every number must appear in the packet. Certification, compliance, guarantee, root-cause and loss claims are rejected. Rejected claims and the reason are stored, and an accepted narrative creates a new immutable report revision.
- Findings are persisted to `evals.finding`/`finding_evidence` when a report is created. Reports list a methodology limitation when follow-up turns or tool results were scripted (see WP-10).
- The workspace report page has publish, PDF/CSV/CEF export, share-with-preview, revoke, compare and narrative actions.
- Production: report `bd652ef3…` was published automatically, the PDF (26 KB, `%PDF`) and CSV exports completed, and the bearer share exposed only the allowlisted fields and returned 404 after revocation. A narrative revision (`0845a7a8…`) completed with two unsupported claims rejected. A later narrative attempt with malformed model output was rejected as `narrative_output_schema_invalid`, leaving the deterministic takeaways unchanged.
