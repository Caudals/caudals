# WP-10 — Multi-turn and deterministic tools

Status: deployed. A production multi-turn correction scenario ran end to end through the deployed worker and passed. Reports disclose scripted follow-up turns. Website conversations need a recipe re-probed with the follow-up check (WP-09).

## Implemented locally

- Frozen scenario graphs support scripted clarification/correction turns, bounded visits, conditional termination, deterministic simulated tool results, final-state predicates and tool traces. A tool schema/transition failure has its own reason code, separate from connector transport errors.
- The runner checks case/run/target/attempt identity and transcript continuity at every turn. Turn, tool, deadline and cumulative output limits stop loops; unavailable provider token usage is recorded as estimated for the local stop, not reported as measured usage.
- The Stage C tests exercise correction, missing-information clarification, tool-error recovery, invalid identity, tool budget, output budget and final-state grading. Comparison checks include frozen policy compatibility.
- A successfully executed but failed or partially failed observation can create a `regression_case` record in pending redaction/validation state; transport errors, passing and unassessed interactions are rejected.
- A platform operator can inspect the failed interaction and source case via `GET /api/evals/v1/regression-cases/{id}`, add a new, source-backed case revision linked to that draft, then call `POST /api/evals/v1/regression-cases/{id}/release` with explicit redaction and validation confirmations. Release requires the named reviewer, locally rechecks the old failed answer against deterministic graders, rejects obvious addresses/credentials and records an audit event. Freezing any suite containing a regression-linked revision requires that exact revision to be released and validated; no endpoint adds it to a suite automatically.

## Release gates still open

- Reviewer confirmation and pattern checks do not replace a full privacy review before using customer interactions.
- Closed 2026-09-25 (see production closure): a bounded multi-turn scenario ran against a Caudals-controlled production target through the deployed worker. Original gate: run a bounded multi-turn scenario and then verify the customer run projection. The disposable PostgreSQL worker/adapter fixture now covers two turns, malformed tool call versus synthetic network failure, tenant isolation and per-turn accounting; production scenario acceptance, fixture reset and incompatible comparison remain open. No synthetic fixture may perform a live side effect.
- Closed in code 2026-09-25 (WP-09): website scenarios now keep a persistent browser context within one case attempt and a verified reset between attempts. Until that exists, the website capability and run gate mark multi-turn cases unsupported rather than scoring a conversation assembled from separate contexts.
- Closed 2026-09-25: report methodology discloses scripted follow-up turns and estimated usage.

## Per-turn target accounting rollout — 2026-09-25

Migration 057 adds a tenant-scoped `target_invocation_call` record committed before each outbound request. The existing attempt row remains the session reservation. A recorded call retains reported usage; a transport exception or lease recovery leaves that call `unknown`. A scenario failure after recorded tool output does not erase the completed call. Customer run reads count actual calls, with a legacy fallback for attempts predating migration 057. The worker serializes requests-per-minute claims across replicas and uses a fixed reason code for uncertain transport, without storing raw exception text.

The migration was applied under the evaluation migration lock after a fresh root-only database and global-role backup, with general, browser and scheduler dispatch paused and no active target attempts. Its SHA-256 is recorded in production migration history. Commit `dcc9eb3` passed TypeScript, targeted ESLint, the 25-test Stage C suite and a non-owner PostgreSQL fixture exercising single, two-turn, malformed-tool and network-failure worker paths. GitHub Actions CI, Secret Scan, app image build/deploy and evaluation image build/deploy all passed for that commit. App, worker, browser and scheduler run immutable digests from the release; all 16 Swarm services returned to 1/1, and the app, evaluation workspace and Leads health URLs returned HTTP 200. No new production target call was made for this accounting rollout, so the production multi-turn release gate above remains open.

## Regression release acceptance — 2026-09-24

A fresh disposable PostgreSQL 16 database with migrations 031–049 and a login inheriting only `evals_runtime` exercised two concurrent draft requests, a pending freeze rejection, cross-workspace denial, obvious personal-address rejection, non-deterministic-grader rejection, idempotent release, local regrading of the failed answer and a successful frozen suite. The test initially exposed an `ON CONFLICT DO UPDATE` on the source observation field that the runtime role cannot change; commit `68d01a3` uses `DO NOTHING` and reads the existing row within the same transaction. Typecheck, lint, build and the 528-test general suite passed; the targeted database fixture passed separately.

After GitHub CI and image deployment passed, an authenticated synthetic operator used the production API to create regression draft `c5900277-707a-4e89-919e-06b27a4fafa8` from the Caudals-owned failed browser observation, add a source-backed revision and release it. Local deterministic revalidation returned `fail`, as expected for the mock chatbot's echoed answer. Suite version `904adb4f-66ca-4105-b84c-136c83199a74` froze with that released revision. The temporary identity was then disabled; database checks found zero sessions, zero platform roles and no password. No customer data or external system was involved.

## Production closure — 2026-09-25 (UTC)

- The API run endpoint queues frozen `conversation` cases with their scenario when the target's ready connection check reports `multi_turn: supported` (WP-04).
- Production acceptance, run by `session-acceptance.cjs multiturn` in the synthetic workspace:
  1. The script built the Caudals-owned multi-turn correction fixture with the production hashing code, froze it with the source artifact in the manifest, and created a target revision allowing 4 turns (the existing revision allowed 1, so its first attempt stopped with `turn_budget_exhausted`). The new revision passed its connection check.
  2. Run `72a300c1-b997-46ae-acc3-b2f7798df09c` completed with 2 recorded target calls and transcript `user, assistant, user, assistant`.
  3. The final answer was `EUR 220.00` after the scripted correction, and the assessment was `pass`.
- Reports now list the limitation that follow-up user turns and tool results were scripted from the frozen tests, and that per-turn usage is estimated where the system did not report it.
- Still open: a full privacy review before any customer interaction becomes a regression case (the reviewer confirmation remains necessary but not sufficient).
